{
  "name": "otp-ai-mcp",
  "description": "otp-ai-mcp \u2014 Built by [MEOK AI Labs](https://meok.ai)",
  "version": "1.0.0",
  "tools": [
    {
      "name": "generate_secret",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "issuer": {
            "type": "string"
          }
        },
        "required": []
      }
    },
    {
      "name": "generate_totp",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "secret": {
            "type": "string"
          }
        },
        "required": [
          "secret"
        ]
      }
    },
    {
      "name": "verify_totp",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "secret": {
            "type": "string"
          },
          "code": {
            "type": "string"
          }
        },
        "required": [
          "secret",
          "code"
        ]
      }
    },
    {
      "name": "generate_qr_uri",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "secret": {
            "type": "string"
          },
          "account": {
            "type": "string"
          },
          "issuer": {
            "type": "string"
          }
        },
        "required": [
          "secret",
          "account"
        ]
      }
    }
  ]
}
